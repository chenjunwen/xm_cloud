eureka:
  client:
    service-url:
      defaultZone: http://localhost:9527/eureka
server:
  port: 9530

spring:
  application:
    name: service-zuul

zuul:
  routes:
    api-ribbon:
      path: /api-a/**
      serviceId: service-ribbon
    api-b:
      path: /api-b/**
      serviceId: service-feign



